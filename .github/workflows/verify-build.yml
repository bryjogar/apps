name: Build verify

on:
  push:
    branches: [ "main" ]
    paths:
      - 'verify/**'  # Triggers only when this specific folder changes

# CRITICAL ADDITION: Permissions needed to upload Release assets
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x verify/gradlew

      # 1. Decode Keystore from Secrets
      # Note: We decode into the 'app' folder so it's easy for Gradle to find
      - name: Decode Keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > verify/app/release.jks

      # 2. Build the Signed APK
      - name: Build with Gradle
        run: cd verify && ./gradlew assembleRelease
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

      # 3. Rename APK (to avoid generic 'app-release.apk')
      - name: Rename APK
        run: mv verify/app/build/outputs/apk/release/app-release.apk verify/app/build/outputs/apk/release/verify.apk

      # 4. Upload to "verify-latest" Release
      - name: Release APK
        uses: softprops/action-gh-release@v1
        with:
          tag_name: verify-latest
          name: "verify (Latest)"
          files: verify/app/build/outputs/apk/release/verify.apk
          overwrite: true
